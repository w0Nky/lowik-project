<?php
	/**** SESSION ****/
	//session_start();

	/**** CLASS CONTROLEUR ****/
    //require_once('class/t_texte.php');

	//require_once('class/c_session.php');

	/**** MODELE ****/
	//require_once('modele/m_session.php');
	//require_once('modele/m_publication.php');
	//require_once('modele/m_campagne.php');
	
	/**** OBJETS ****/
    //$t_texte = new t_texte();

	//$m_session = new m_session($base_de_donnee);
	//$c_session = new c_session($m_session, $t_texte);

    //$m_publication = new m_publication($base_de_donnee);
    //$m_campagne = new m_campagne($base_de_donnee);

	/**** VERIF SESSION ****/
	//$c_session->session();

	//if(!empty($url_param[0])) {
		//if(preg_match('#^[0-9]{1,}$#', $url_param[0])) {
			//$num_page = $url_param[0];
		//} else { $num_page = 1; }
	//} else { $num_page = 1; }

    //$liste_publications = $m_publication->liste_publication($num_page);
?>